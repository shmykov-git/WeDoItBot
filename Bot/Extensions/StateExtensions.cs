using System.Linq;
using Bot.Model;

namespace Bot.Extensions
{
    public static class StateExtensions
    {
        //public static bool CanGo(this State state, string key)
        //{
        //    return state.CurrentRoom?.Artifacts.Any(a => a.Go == key) ?? true;
        //}
    }
}